{% macro defraUnorderedList(params, renderDefraComponent) %}
  {%- set items = params.items or [] %}
  {%- set classes = params.classes or 'govuk-list govuk-list--bullet' %}
  {%- set spaced = params.spaced or false %}
  {%- set id = params.id or '' %}

  <ul{% if id %} id="{{ id }}"{% endif %} class="{{ classes }}{% if spaced %} govuk-list--spaced{% endif %}">
    {% for item in items %}
      <li>
        {% if item.component %}
          {{ renderDefraComponent(item) }}
        {% else %}
          {{ item.text | safe }}
        {% endif %}
      </li>
    {% endfor %}
  </ul>
{% endmacro %}