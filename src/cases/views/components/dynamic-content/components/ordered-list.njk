{% macro defraOrderedList(params, renderDefraComponent) %}
  {%- set items = params.items or [] %}
  {%- set classes = params.classes or 'govuk-list govuk-list--number' %}
  {%- set id = params.id or '' %}

  <ol{% if id %} id="{{ id }}"{% endif %} class="{{ classes }}">
    {% for item in items %}
      <li>
        {% if item.component %}
          {{ renderDefraComponent(item) }}
        {% else %}
          {{ item.text | safe }}
        {% endif %}
      </li>
    {% endfor %}
  </ol>
{% endmacro %}