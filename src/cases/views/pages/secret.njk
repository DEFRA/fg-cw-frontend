{% extends '../layouts/case-layout.njk' %}
{% from "govuk/components/tabs/macro.njk" import govukTabs %}
{% from "components/case-tabs/macro.njk" import caseTabs %}
{% from "checkbox/macro.njk" import checkbox %}
{% from "govuk/components/button/macro.njk" import govukButton %}

{% block content %}
  {{ heading({ text: pageHeading }) }}

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-full">
      <div class="govuk-body" data-testid="app-page-body">
        <h1>Logged in!</h1>
        <pre style="padding:1rem;max-width:600px;white-space:pre-wrap;">
{{ data | dump(2) }}</pre
        >
        {{
          govukButton({
            text: "Logout",
            href: "/logout"
          })
        }}
      </div>
    </div>
  </div>
{% endblock %}
